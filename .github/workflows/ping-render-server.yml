name: Ping Render Server

on:
  schedule:
    - cron: "*/5 * * * *"  # Hvert 5. minut

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send ping til Render backend
        run: |
          curl -s https://kulturskole-infosk-rm.onrender.com/ping
